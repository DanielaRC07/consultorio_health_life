version: '3'

services:
  db_consultorio:
    image: mysql:5.7
    container_name: db_consultorio
    environment:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_DATABASE: login_consultorio
      MYSQL_USER: daniel
      MYSQL_PASSWORD: 36758402
    ports:
      - "3306:3306"
    volumes:
      - dbdata:/var/lib/mysql

volumes:
  dbdata:
